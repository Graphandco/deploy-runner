name: Build Deploy Runner

on:
   workflow_dispatch:
   push:
      branches: [main]
      paths:
         - "apps/admin-dashboard/deploy-runner/**"
         - ".github/workflows/deploy-runner.yml"

env:
   REGISTRY: ghcr.io
   IMAGE_NAME: ghcr.io/graphandco/deploy-runner

jobs:
   build-and-push:
      runs-on: ubuntu-latest
      permissions:
         contents: read
         packages: write

      steps:
         - uses: actions/checkout@v4

         - name: Log in to GitHub Container Registry
           uses: docker/login-action@v3
           with:
              registry: ${{ env.REGISTRY }}
              username: ${{ github.actor }}
              password: ${{ secrets.GITHUB_TOKEN }}

         - name: Build and push
           uses: docker/build-push-action@v6
           with:
              context: ./apps/admin-dashboard/deploy-runner
              push: true
              tags: |
                 ${{ env.IMAGE_NAME }}:latest
                 ${{ env.IMAGE_NAME }}:${{ github.sha }}
